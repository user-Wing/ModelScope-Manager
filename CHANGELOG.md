# Changelog

ModelScope Manager 变更记录。格式遵循 Keep a Changelog 分类（新增 / 修复 / 变更 / 安全），**新版本在上**；条目为面向使用者的简要描述，完整原始细节见各版本 `V1.0.x 更新日志.md`。日期以 GitHub Release 为准。

## [1.0.8] - 2026-09-17

[Release](https://github.com/user-Wing/ModelScope-Manager/releases/tag/1.0.8)

### 新增
- 资源管理主页右上角新增「从链接导入文件」：一行一个 HTTP(S) 链接，先下载到本机临时目录，再使用当前仓库对应的 Token 账户上传到当前目录；任务结束后自动清理临时文件，并对大文件给出谨慎使用提示。
- 设置页新增「插件下载」表格，集中管理 PotPlayer 与 FFmpeg。PotPlayer 原下载入口迁移到这里；FFmpeg 固定从 `ARXChem/Software-List/ffmpeg/FFmpeg.7z` 下载，按大小与 SHA-256 校验后解压到 `embedded-tools/ffmpeg/`，供图床 AVIF 自动转换优先调用。
- 左侧设置按钮上方新增「关于」页面（皮肤编号 `-1`），使用 Markdown 展示软件更新日志、第三方开源库引用与支持者列表；当前支持者列表为空。
- 新增左侧「WebDAV 映射」页面（皮肤编号 6）：可创建命名 WebDAV 根目录和虚拟文件夹，把 `/models`、`/datasets`、`/public` 下任意文件或目录重复组合挂载；默认只读，关闭保护需二次确认。
- 下载遇到本地同名文件时新增「强制覆盖 / 恢复模式 / 不覆盖」选择；恢复模式按大小与 SHA-256 复用完整文件，并把部分文件交给 aria2 续传。
- 文件详细信息表头右键新增「选择列」，可显示 ModelScope GET 列表返回的最后修改时间、Git/LFS 存储方式、SHA-256 与 Blob ID；名称、类型、大小固定保留。
- 远端 Range 压缩包浏览扩展到 ZIP、TAR、ISO、GZ、XZ；TAR 逐头跳过文件内容，GZ/XZ 按单流格式显示元数据，RAR 明确不支持。
- 皮肤改为库式管理：`data/skin/` 的每个子目录是一套可独立切换、重命名的皮肤，旧版根目录皮肤会自动迁移。
- 支持导入皮肤文件夹及 ZIP/7z 压缩包，并可将当前皮肤默认导出到软件目录下的 `[皮肤名称].zip`。
- 主题设置独立成栏，支持多背景按页面映射、逐图亮度与随机模式；设置页编号为 0，六个顶部页面依次为 1–6。
- 皮肤包升级为“多图片 + `skin.ini`”文件夹格式，支持颜色模式、自动/自定义取色、页面亮度映射以及顶部/底部文字模板。
- 顶部与底部文字新增 `[US]`、`[DS]`、`[AU]`、`[AD]`、`[CU]`、`[CD]`、`[VER]`、`[CPU]`、`[RAM]`、`[RT]` 通配符。
- 常规设置支持导出和导入 INI；皮肤、实验性功能、凭据与未完成传输队列不会导出。
- 外观设置支持分别选择西文字体与中文回退字体，切换后立即作用于 Qt 控件与统计图表。
- 新增自定义主题：背景图片、自动取色/手动主题色、实时亮度调节，以及包含 `skin.ini` 和图片的皮肤包导入；背景图限制为 10 MB。
- 新增桌面、当前用户/所有用户开始菜单、「此电脑」快捷入口，并可一键清理；涉及 ProgramData 的操作按需请求 UAC。
- 可在设置中请求管理员权限配置 Windows WebClient（Basic Auth、文件和目录属性上限），并把本机 WebDAV 根目录直接映射为网络驱动器。
- 公开资源链接支持携带仓库子目录；同一仓库的不同子目录可分别保存、索引并在 WebDAV `/public` 下独立挂载。
- 远端 `.7z` 文件新增 Range 目录探测，不完整下载压缩包即可尝试读取目录；WebDAV 保留 Range/直链重定向，供 AList 的压缩包浏览使用。
- 传输页新增历史记录，持久保存上传/下载源、目标、成功状态、耗时、完成时间与错误信息。
- 未完成的上传/下载队列和断点在退出时保存，重新启动后可继续；关闭程序会停止 aria2、缩略图 FFmpeg 与后台 Python 工作线程。

### 变更
- PotPlayer 的下载、校验与安装入口从媒体播放设置迁移到统一的「插件下载」区域；播放设置保留默认播放器开关与第三方播放器列表。
- 皮肤页面编号扩展为 `-1`–`6`：关于=-1、设置=0，原资源管理至 WebDAV 映射继续使用 1–6，旧皮肤的全页面迁移会自动包含关于页。
- 状态栏右侧皮肤文字按实际内容与可用窗口宽度自动适配，不再固定限制为窗口的 62%。
- 主题亮/暗/跟随系统切换时将当前皮肤的页面亮度重置为 100%，并重新刷新导航栏、底栏和浅色半透明层。
- 皮肤图片的“应用到页面”两列均可拖动宽度；复选框及其文字统一向右留出 4 px 间距。
- 补全本次新增 WebDAV 映射、下载恢复、压缩浏览、可选列及近期主题/更新设置的英文翻译。
- 底部自定义文字改为状态栏右侧永久区域，保持从左到右排版；AList、aria2 等临时状态固定使用左侧区域，互不遮挡。
- 启动阶段提前安装最终主题样式，并将 `requests`、ModelScope 上传 SDK 和仓库/网关任务延后到按需或首帧完成后加载。
- `skin.ini` 新增 `skin_name`；设置页“应用到页面”直接编辑 `页面#亮度`，同一图片可为不同页面保留不同亮度。
- 窗口顶部默认只显示软件名称，不再固定附加版本号；版本可通过 `[VER]` 按需加入顶部或底部文字。
- 启动时先设置 Fluent 全局主题再创建控件，并在恢复设置期间抑制重复主题重绘。
- 下载总进度和单文件状态分为 `X%-下载`、`X%-校验` 两个阶段，大文件 SHA-256 校验按已读取字节实时更新。
- WebDAV 下载先安全解析 ModelScope 对象存储签名地址，再把直链重定向给 AList，避免由本地 Python 网关中转大文件。
- Windows 直挂仅允许 `127.0.0.1`/`localhost`，避免 HTTP Basic Auth 凭据进入局域网。

### 修复
- 修复 Edge WebView2 网页登录偶发只显示白色窗口的问题：私密登录会话现在显式持有独立临时 WebView2 用户数据目录直到窗口关闭，再自动清理。
- 调整资源主页详细列表的复选框水平留白，使复选框在列表左边界与首列文字之间保持居中。
- WebDAV 自定义挂载从网页登录账户发起时会先检查同用户名、同仓库的 Token 账户：存在时自动改用 Token 挂载凭据并提示，不存在时阻止挂载并说明网页登录账户仅用于补全删除功能。
- 修复内置 `modelscope_hub 0.2.0` 在 ModelScope 用户信息接口返回 `name` 等兼容字段时无法取得用户名、导致有效 Token 无法登录的问题。
- 补完 WebDAV 自定义映射的虚拟文件夹编辑：可新增、重命名/移动和删除目录，并统一按 Windows WebDAV 的大小写不敏感语义解析挂载路径；载入配置时同步过滤非法路径和大小写冲突。
- 修复 AList 网关启动提示或 aria2 状态出现时，底部自定义文字被隐藏或与临时消息重叠的问题。
- 修复启动恢复设置时多次触发全局 Fluent 主题重绘、导致 `start.bat` 后需等待 5–10 秒才出现窗口的问题；主题现在在控件创建前预设，恢复期间不重复重绘，GPU 资源探测延后到窗口显示之后。
- 修复 Windows 系统存在 `webclnt.dll`、但 WebClient 服务的 `ServiceDll` 注册项缺失时仍提示“系统找不到指定的文件”的问题；管理员配置现在会校验组件并恢复服务注册项后再启动。
- 下载与历史记录页面的所有表头列改为可拖动调宽，并保留适合完整路径的初始宽度。
- 设置页 WebDAV「监听端口」控件增加右侧留白，避免在窄窗口或滚动区域边缘超出卡片范围。
- 修复 Windows WebDAV 管理员配置写入 DWORD 最大值时 PowerShell 返回代码 1、导致盘符无法映射的问题；改用 `reg.exe` 写入，并显示 UAC 内层的真实错误信息。
- 修复自定义背景图被 Fluent 页面不透明底色遮挡的问题；背景现在按比例铺满整个窗口，导航、卡片、列表和输入区域使用半透明内容层。
- 修复选择背景图和拖动亮度时反复解码、重写 PNG 与重载全局样式导致的明显卡顿；现在只缓存一次原图并实时调整轻量遮罩。
- 修复超过 2 GiB 的下载/校验字节数通过 Qt 32 位整数信号时溢出为负数、阻断校验流程的问题。
- 修复 aria2 多段下载创建稀疏文件后，界面误用文件长度并瞬间跳到 99% 的问题。
- 修复 aria2 隐藏已完成任务导致累计进度回落、统计分析漏记后续文件下载量的问题。
- 修复断点续传启动时把上次已下载的部分再次计入本次统计的问题；现在先读取 aria2 断点作为基线，只累计本次新接收字节。
- 下载队列的远端资源和本地位置列按完整内容扩展，不再强制显示省略号。

## [1.0.7] - 2026-09-13

[Release](https://github.com/user-Wing/ModelScope-Manager/releases/tag/1.0.7)

### 新增
- 设置页最底部新增「实验性功能」栏目：可选择明文保存密码、Token 与网页登录 Cookie，或关闭跨设备凭据自动销毁。
- 启用两个凭据安全实验选项前，必须先确认风险，再正确完成随机四位数加四位数计算；任一实验选项启用时，窗口顶部持续显示「软件运行在高风险测试模式下」。
- 资源管理器顶部路径栏支持把本地文件或文件夹直接拖到根目录、可见子目录；路径过长时可拖到省略按钮展开的二级目录菜单，悬停提示实际上传目标。
- 资源管理器在可写仓库的当前目录提供「上传文件」「上传文件夹」，并支持复选框、Shift 连选和 Ctrl 逐项多选；复制链接、下载、标签、复制，以及网页登录账户的删除和移动均可批量执行。

### 变更
- 启用明文凭据模式时会迁移当前可解密的 Token 和网页登录会话；WebDAV 密码也按相同模式保存。恢复 DPAPI 模式时重新加密可读取的凭据。
- 经内置 `modelscope_hub 0.2.0` 与 ModelScope 数据集 LFS 接口实测，51 GiB 对象初始化请求可被接受并返回上传地址；软件不再拦截 50 GB 以上文件，低于 SDK 100 GB 上限的上传默认启用。
- 传输列表的上传页移除目标路径输入和拖放/选择入口，扩大队列列表；新增上传只从资源管理器发起并明确绑定当前目录。
- 「搜索已索引资源」弹窗改为后台查询、220 ms 输入防抖与每批 400 项渲染；名称、类型、大小、仓库和路径表头均可切换正序/倒序。
- 实时上传/下载速度移到窗口版本号右侧；高风险实验开启时在其后显示紧凑的「测试模式」状态，不再占用整行空间。
- 统计分析页以时间段内上传总量、下载总量和已学习上传速度作为核心指标，并放大开始/结束时间控件。

### 修复
- 修复资源管理器未选中项目时仍可点击下载、并可能误用另一个视图残留选择的问题；空选时下载按钮现在保持禁用。
- 修复大量索引结果搜索时主界面明显卡顿，以及「搜索已索引资源」弹窗表头点击事件未启用、导致结果始终按名称正序的问题。
- 修复 Fluent 筛选框的数据被误作图标参数、导致已索引资源始终查询为 0 项的问题。
- 调整文件列表复选框与「名称」表头对齐，并增加复选框和文件名之间的留白。
- 资源页「分组依据」及索引搜索弹窗的三个筛选下拉框改用无弹出动画的 Fluent 控件，消除圆角边缘锯齿和残影。

### 安全
- 两项高风险选项默认关闭；默认行为仍是 DPAPI 加密、设备绑定与设备变化后销毁凭据。
- 单独关闭跨设备销毁不会绕过 DPAPI 的系统账户边界；跨设备直接读取凭据还需同时启用明文保存，界面会明确告知风险。

## [1.0.6] - 2026-09-11

[Release](https://github.com/user-Wing/ModelScope-Manager/releases/tag/1.0.6)

### 新增
- 设置页新增 ModelScope 软件更新：自动/手动检查 `ARXChem/Software-List` 文件列表，使用内置 aria2-next 下载、内置 7z 解压，退出后替换程序文件并保留 `data/`。
- 图床新增可选「自动转换为 AVIF」与详细配置，在分享前以 q70/speed 5 默认预设压缩图片，也可调整质量、速度、色度、位深和元数据。
- 设置页新增本进程 CPU、内存与显存监控，并支持手动或后台空闲时自动释放工作集。

### 变更
- 便携启动与开机自启使用 Python `-s` 隔离模式，确保只加载软件内置 SDK 与 PySide6 依赖。
- 合并完整版的测试与历史说明资产，保留 Cut 版较新的业务代码和本地数据。
- 将超大主窗口文件拆分为独立的页面、后台任务和业务模块，保持原有导入接口不变，降低后续功能维护与定位成本。
- 网页登录由 QtWebEngine 改为系统 Edge WebView2 私密窗口，减少便携包体积并继续支持自动获取登录会话。
- AVIF 转换按需探测内置或系统 FFmpeg；组件缺失时原图上传不受影响。
- 自动内存回收仅在软件处于后台、没有传输任务且超过用户阈值时触发，避免影响前台操作。

### 安全
- WebView2 登录使用临时私密配置，捕获完成即清除 Cookie；密码和验证码仍只提交给 ModelScope 官方页面。

### 移除
- 移除 QtWebEngine 代码与约 209 MiB 运行时文件，主界面继续使用轻量的 PySide6 Core/Gui/Widgets。

## [1.0.5] - 2026-09-05

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.5)

### 新增
- 传输统计分析页面：实时上传/下载速度、会话流量、平均速度、峰值与时序图表
- 上传速度学习与持续慢速自动重连
- 文件夹上传支持多文件并发，各任务进度独立统计
- WebDAV 新增 `/dav/` 入口（原根地址兼容），支持 `/models`、`/datasets`、`/public` 子目录作为挂载根

### 修复
- 大型数据集目录长时间无法加载：文件列表分页从每页 100 提升至接口支持的 3000
- WebDAV Basic Auth 重试时残留请求体破坏 HTTP 连接
- 设置页展开、下拉菜单与主题切换后的残影

### 变更
- 补全新增界面与状态文本的英文翻译

## [1.0.4]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.4)

### 安全
- Token/Cookie 仅发送给 ModelScope 官方域；跨域重定向自动剥离认证头（缩略图、下载、WebDAV、媒体代理全部接入）
- 网页登录 Cookie 显式绑定 `.modelscope.cn` 作用域
- aria2 不再将含 Token 的任务清单写入磁盘，改由仅监听 `127.0.0.1` 的随机密钥 RPC 注入
- 上传与备份拒绝符号链接 / junction，并在读取前校验文件仍处于所选根目录内
- Token 加密默认仅当前用户可解（不再静默降级为机器级）；旧凭据无损迁移
- Token / 网页会话在系统加密不可用时仅保存在内存，不写盘
- WebDAV 保持内网 HTTP 设计，增加并发与临时空间保护（最多 2 并发 PUT、预留 512 MiB 空闲）
- 同一仓库多账户可见时，写操作优先选择真实持有 Token 的账户
- 未知可见性 fail-closed，不再误判为公开
- 备份失败按 30 秒 ~ 5 分钟退避重试，不再误记为成功扫描
- 备份上传前后复核 size/mtime，避免把变化中的文件误记"已备份"
- 增量备份可从多个时间戳目录重建当前完整文件集合（老任务回退原逻辑）

### 修复
- 单文件下载时"本地位置"列显示不清
- 下载任务状态不更新（Windows 路径写法差异也能正确匹配）
- 下载刚启动时总进度条立即显示 100%（下载与校验中最多显示 99%）
- 已有任务上传中继续拖放无法追加到上传队列

## [1.0.3]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.3)

### 新增
- 多网页登录账户（设置 → 账号）：登录会话 DPAPI 设备绑定加密，不以明文写盘
- 程序内删除：不可逆确认、文件夹递归收集、100 文件/次合并提交、结果不确定时回查核对
- 移动与区分大小写的重命名：先传后删，任一步失败保留原文件
- 资源管理器区分 Public / Token / 网页登录三类账户来源

### 变更
- 删除后增量更新 SQLite 条目、标签与目录大小，不再全量重扫仓库

### 修复
- 深色模式下上传百分比文字对比度过低
- 设置页透明菜单残影；控件对齐与间距

### 安全
- 网页登录仅用于身份、读取、下载与删除；上传一律走目标仓库有权访问的 Token SDK

## [1.0.2]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.2)

### 变更
- 设置页升级 Fluent Design 风格：恢复中英切换、浅/深/跟随系统主题、关闭窗口询问
- 复杂设置改为统一折叠卡片 + 180ms 缓动动画；深色模式按钮恢复清晰 Fluent 图标
- 缩略图生成提速：视频优先定位约 1.5 秒取帧、缓存源图 320×180、提高并发
- 图床按账户记住上次使用的仓库
- 搜索移除 500 项截断，支持 Everything 风格高级搜索（`path:`/`name:`/`ext:`/`type:`、引号、多关键词 AND、通配符）与表头排序
- 优化大目录浏览、缩略图队列与搜索防抖（默认更高并发换取更快的预览与搜索反馈）

### 修复
- 浅色模式下拉菜单黑块 / 黑影
- WebDAV 空闲端口被误报"未找到端口"；端口冲突自动寻找相邻可用端口并保存实际值
