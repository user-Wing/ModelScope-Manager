# Changelog

ModelScope Manager 变更记录。格式遵循 Keep a Changelog 分类（新增 / 修复 / 变更 / 安全），**新版本在上**；条目为面向使用者的简要描述，完整原始细节见各版本 `V1.0.x 更新日志.md`。日期以 GitHub Release 为准。

## [1.0.6] - 2026-09-11

[Release](https://github.com/user-Wing/ModelScope-Manager/releases/tag/1.0.6)

### 新增
- 图床新增可选「自动转换为 AVIF」与详细配置，在分享前以 q70/speed 5 默认预设压缩图片，也可调整质量、速度、色度、位深和元数据。
- 设置页新增本进程 CPU、内存与显存监控，并支持手动或后台空闲时自动释放工作集。

### 变更
- 合并完整版的测试与历史说明资产，保留 Cut 版较新的业务代码和本地数据。
- 将超大主窗口文件拆分为独立的页面、后台任务和业务模块，保持原有导入接口不变，降低后续功能维护与定位成本。
- 网页登录由 QtWebEngine 改为系统 Edge WebView2 私密窗口，减少便携包体积并继续支持自动获取登录会话。
- AVIF 转换按需探测内置或系统 FFmpeg；组件缺失时原图上传不受影响。
- 自动内存回收仅在软件处于后台、没有传输任务且超过用户阈值时触发，避免影响前台操作。

### 安全
- WebView2 登录使用临时私密配置，捕获完成即清除 Cookie；密码和验证码仍只提交给 ModelScope 官方页面。

### 移除
- 移除 QtWebEngine 代码与约 209 MiB 运行时文件，主界面继续使用轻量的 PySide6 Core/Gui/Widgets。

## [1.0.5] - 2026-09-05

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.5)

### 新增
- 传输统计分析页面：实时上传/下载速度、会话流量、平均速度、峰值与时序图表
- 上传速度学习与持续慢速自动重连
- 文件夹上传支持多文件并发，各任务进度独立统计
- WebDAV 新增 `/dav/` 入口（原根地址兼容），支持 `/models`、`/datasets`、`/public` 子目录作为挂载根

### 修复
- 大型数据集目录长时间无法加载：文件列表分页从每页 100 提升至接口支持的 3000
- WebDAV Basic Auth 重试时残留请求体破坏 HTTP 连接
- 设置页展开、下拉菜单与主题切换后的残影

### 变更
- 补全新增界面与状态文本的英文翻译

## [1.0.4]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.4)

### 安全
- Token/Cookie 仅发送给 ModelScope 官方域；跨域重定向自动剥离认证头（缩略图、下载、WebDAV、媒体代理全部接入）
- 网页登录 Cookie 显式绑定 `.modelscope.cn` 作用域
- aria2 不再将含 Token 的任务清单写入磁盘，改由仅监听 `127.0.0.1` 的随机密钥 RPC 注入
- 上传与备份拒绝符号链接 / junction，并在读取前校验文件仍处于所选根目录内
- Token 加密默认仅当前用户可解（不再静默降级为机器级）；旧凭据无损迁移
- Token / 网页会话在系统加密不可用时仅保存在内存，不写盘
- WebDAV 保持内网 HTTP 设计，增加并发与临时空间保护（最多 2 并发 PUT、预留 512 MiB 空闲）
- 同一仓库多账户可见时，写操作优先选择真实持有 Token 的账户
- 未知可见性 fail-closed，不再误判为公开
- 备份失败按 30 秒 ~ 5 分钟退避重试，不再误记为成功扫描
- 备份上传前后复核 size/mtime，避免把变化中的文件误记"已备份"
- 增量备份可从多个时间戳目录重建当前完整文件集合（老任务回退原逻辑）

### 修复
- 单文件下载时"本地位置"列显示不清
- 下载任务状态不更新（Windows 路径写法差异也能正确匹配）
- 下载刚启动时总进度条立即显示 100%（下载与校验中最多显示 99%）
- 已有任务上传中继续拖放无法追加到上传队列

## [1.0.3]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.3)

### 新增
- 多网页登录账户（设置 → 账号）：登录会话 DPAPI 设备绑定加密，不以明文写盘
- 程序内删除：不可逆确认、文件夹递归收集、100 文件/次合并提交、结果不确定时回查核对
- 移动与区分大小写的重命名：先传后删，任一步失败保留原文件
- 资源管理器区分 Public / Token / 网页登录三类账户来源

### 变更
- 删除后增量更新 SQLite 条目、标签与目录大小，不再全量重扫仓库

### 修复
- 深色模式下上传百分比文字对比度过低
- 设置页透明菜单残影；控件对齐与间距

### 安全
- 网页登录仅用于身份、读取、下载与删除；上传一律走目标仓库有权访问的 Token SDK

## [1.0.2]

[Release](https://github.com/user-Wing/ModelScpoe-Manager/releases/tag/1.0.2)

### 变更
- 设置页升级 Fluent Design 风格：恢复中英切换、浅/深/跟随系统主题、关闭窗口询问
- 复杂设置改为统一折叠卡片 + 180ms 缓动动画；深色模式按钮恢复清晰 Fluent 图标
- 缩略图生成提速：视频优先定位约 1.5 秒取帧、缓存源图 320×180、提高并发
- 图床按账户记住上次使用的仓库
- 搜索移除 500 项截断，支持 Everything 风格高级搜索（`path:`/`name:`/`ext:`/`type:`、引号、多关键词 AND、通配符）与表头排序
- 优化大目录浏览、缩略图队列与搜索防抖（默认更高并发换取更快的预览与搜索反馈）

### 修复
- 浅色模式下拉菜单黑块 / 黑影
- WebDAV 空闲端口被误报"未找到端口"；端口冲突自动寻找相邻可用端口并保存实际值
