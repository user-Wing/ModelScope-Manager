# ModelScope Manager 自定义皮肤

## 皮肤库与切换

打开「设置 → 主题设置」，可在“当前皮肤”下拉框中切换已经安装的皮肤，并直接修改“皮肤名称”。名称修改后会立即写回当前皮肤的 `skin.ini`。

软件把 `data/skin/` 作为皮肤库根目录，每个直接子目录是一套独立皮肤：

```text
data/skin/
├─ dark-space/
│  ├─ skin.ini
│  ├─ home.jpg
│  └─ transfer.png
└─ light-paper/
   ├─ skin.ini
   └─ paper.webp
```

旧版直接放在 `data/skin/` 根目录的 `skin.ini` 和图片会在首次启动时自动迁移到 `data/skin/current/`，原有设置与图片不会丢失。

## 导入与导出

- “导入文件夹”选择一个直接包含 `skin.ini` 和图片的文件夹。
- “导入 ZIP/7z”支持 `.zip`、`.7z` 皮肤包。压缩包应包含一个皮肤文件夹，该文件夹内放置 `skin.ini` 和图片。
- 导入后软件会在 `data/skin/` 下创建一个独立且不重名的子目录，并立即切换到新皮肤。
- “导出 ZIP”把当前皮肤的整个子目录打包。默认输出到软件所在目录，文件名为 `[皮肤名称].zip`，也可以在保存窗口中更改。

每张图片不得超过 10 MB。不存在、无法读取或超过限制的图片会被忽略。

## skin.ini 格式

`skin.ini` 不需要节标题。完整示例：

```ini
skin_name=深空
color_mode=dark
color=auto
auto_color_image=home.jpg
random_bg=0
top_note=ModelScope Manager [VER]
bottom_note=上传速度：[US] | 下载速度：[DS] | CPU：[CPU] | 内存：[RAM]
home.jpg=-1#70,0#45,1#55,2#65
transfer.png=3#80,4#60,5#70
```

字段说明：

- `skin_name`：界面中显示的皮肤名称；在设置中修改后会写回此字段。
- `color_mode`：`light`、`dark` 或 `system`。
- `color`：`auto` 表示从背景自动提取主题色，也可填写 `#RRGGBB` 色号。
- `auto_color_image`：可选；有多张图片时指定自动取色使用的图片。省略时使用第一张有效图片。
- `random_bg`：`1` 开启随机背景，`0` 关闭。
- `top_note`：窗口顶部文字模板；默认不包含版本号。
- `bottom_note`：窗口底部状态栏文字模板。
- 图片行：`图片名称=页面#亮度,页面#亮度`。页面范围为 -1–6，亮度范围为 20–180。

同一图片可对不同页面设置不同亮度，例如 `1.jpg=0#50,1#80,2#35`。设置页表格也直接使用并保留这种写法。只写页码仍兼容，未写亮度时按 `100` 处理。

页面编号如下：

| 编号 | 页面 |
|---:|---|
| -1 | 关于 |
| 0 | 设置 |
| 1 | 资源管理 |
| 2 | 资源搜索 |
| 3 | 传输列表 |
| 4 | 备份文件夹 |
| 5 | 图床 |
| 6 | WebDAV 映射 |

没有分配的页面使用所选颜色模式的默认背景。点击表格左侧行号可选中整行，`+` 添加图片，`−` 删除选中图片；亮度滑块会把选中图片已分配页面的亮度统一调整为当前数值。切换亮色、暗色或跟随系统时，全部已分配页面的亮度会重置为 100%，再按当前模式重新调整即可。

开启随机模式后，每个页面会从当前皮肤中已导入且实际存在的图片随机选择一张。此时只读取图片行左侧的文件名，不读取右侧页面与亮度映射；不存在的图片自动忽略。

## 文字通配符

通配符区分大小写：

| 通配符 | 内容 |
|---|---|
| `[US]` | 当前上传速度 |
| `[DS]` | 当前下载速度 |
| `[AU]` | 软件累计上传量 |
| `[AD]` | 软件累计下载量 |
| `[CU]` | 本次会话上传量 |
| `[CD]` | 本次会话下载量 |
| `[VER]` | 软件版本号 |
| `[CPU]` | 软件当前 CPU 占用 |
| `[RAM]` | 软件当前内存占用 |
| `[RT]` | 软件本次运行时间 |

使用 `|` 分隔状态项，软件会自动按 Fluent Design 的间距显示。底部文字固定在状态栏右侧并保持从左到右排版，AList、aria2 等临时状态显示在左侧。例如：

```ini
bottom_note=上传速度：[US] | 下载速度：[DS]
```

## 常规设置导入导出

“设置导入导出”用于迁移常规设置，不包含皮肤、实验性功能、账号凭据、累计流量和未完成传输队列。皮肤请使用主题设置中的 ZIP 导出功能。
